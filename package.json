{
  "devDependencies": {
    "cypress": "^13.6.1",
    "cypress-mochawesome-reporter": "^3.7.0"
  },
  "scripts": {
    "test": "cypress open",
    "test-headless": "cypress run --browser chrome --headless --spec cypress/e2e/saucedemoUI.cy.js"
  }
}
